#!/usr/bin/env python3
#--*-- coding: utf-8 --*--



# import 

import os, time
from logging import debug, info warning
import logging

from _installer.setting import * 



# fonctions

def user_settings() : 
	input("enable autosettings?")
	input("enable autolaunch ? (DEFAULT and RECOMMANDED")
	input("enable rebooting autorisation? (DEFAULT and RECOMMANDED)" )
	input("log time delta? in minutes default(5)" )
	